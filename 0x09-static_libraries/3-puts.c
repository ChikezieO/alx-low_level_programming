#include "main.h"

/**
 * _puts - Entry point
 *
 * @s: string
 *
 * Return: Always 0 (Success)
 */

void _puts(char *s)

{
_puts("\"Programming is like building a multilingual puzzle");
return (0);
}
